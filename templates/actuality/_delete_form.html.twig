<form method="post" action="{{ path('actuality_delete', {'id': actuality.id}) }}" onsubmit="return confirm('Confirmer la suppression de cette actualité ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actuality.id) }}">
    <button class="btn btn-danger float-left">Supprimer</button>
</form>