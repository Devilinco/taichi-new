{% extends 'base.html.twig' %}

{% block title %}Ajouter un document{% endblock %}

{% block body %}
    <section class="bg-primary text-white h-75" id="Actualites" style="padding-top:110px;">
        <div class="container">
            <div class="row">
                <div class="col-md-12 text-center">
                    <h1>Ajouter un nouveau document</h1>
                    <a class="btn btn-link float-left" href="{{ path('book_index') }}">Revenir à l'index</a>
                </div>
            </div>
            <div class="row mt-4">
                <div class="col-md-12">
                    {{ include('book/_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
{% block javascripts %}
    <script>
        // Add the following code if you want the name of the file appear on select
        $(".custom-file-input").on("change", function() {
            var fileName = $(this).val().split("\\").pop();
            $(this).siblings(".custom-file-label").addClass("selected").html(fileName);
        });
    </script>
{% endblock %}