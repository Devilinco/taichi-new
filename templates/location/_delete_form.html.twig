<form method="post" action="{{ path('location_delete', {'id': location.id}) }}" onsubmit="return confirm('Confirmer la suppression de ce lieu ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location.id) }}">
    <button class="btn btn-danger float-left">Supprimer</button>
</form>
