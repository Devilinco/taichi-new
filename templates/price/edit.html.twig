{% extends 'base.html.twig' %}

{% block title %}Editer un tarif{% endblock %}

{% block body %}
    <section class="bg-primary text-white h-75" id="Locations" style="padding-top:110px;">
        <div class="container">
            <div class="row">
                <div class="col-md-12 text-center">
                    <h1>Editer un tarif</h1>
                    <a class="btn btn-link float-left" href="{{ path('price_index') }}">Revenir à l'index</a>
                </div>
            </div>
            <div class="row mt-4">
                <div class="col-md-12">
                    {{ include('price/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
{#                    {{ include('price/_delete_form.html.twig') }}#}
                </div>
            </div>
        </div>
    </section>
{% endblock %}
